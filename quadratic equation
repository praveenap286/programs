#include <stdio.h>
#include <math.h>
int main()
{
    int a,b,c,z,x,y;
    scanf("%d%d%d",&a,&b,&c);
    z=sqrt((b*b)-(4*a*c));
    x=(-b+z)/(2*a);
    y=(-b-z)/(2*a);
    printf("%d %d",x,y);
    return 0;
}
